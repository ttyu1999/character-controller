// utils.js
const W = [ 'w', 'arrowup' ];
const A = [ 'a', 'arrowleft' ]; 
const S = [ 's', 'arrowdown' ];
const D = [ 'd', 'arrowright' ];
const V = [ 'v', 'x' ]; // 切換視角
const ACTION_1 = [ '1' ];
const ACTION_2 = [ '2' ];
const ACTION_3 = [ '3' ];

const SHIFT = [ 'shift' ];
const SPACE = [ ' ' ];
const ENTER = [ 'enter' ];

const DIRECTIONS = [ W, A, S, D ];

export { W, A, S, D, V, DIRECTIONS, SHIFT, SPACE, ENTER, ACTION_1, ACTION_2, ACTION_3 };